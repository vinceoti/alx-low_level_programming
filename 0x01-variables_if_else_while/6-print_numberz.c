#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int y;

	for (y = 48; y < 58; y++)
		putchar(y);
	
	putchar('\n');
	return (0);
}

