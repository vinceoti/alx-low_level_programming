#include <stdio.h>

/**
 * main - generates possible combs of single-digit numbers
 * Return: 0 for Success
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
		if (i != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
